# Bank Term Deposit Prediction 🏦

## Опис проєкту
Цей проєкт присвячений розробці моделі машинного навчання для прогнозування того, чи оформить клієнт банку строковий депозит (Term Deposit). Мета проєкту — допомогти банківському маркетинговому відділу фокусувати зусилля на найбільш перспективних клієнтах, оптимізуючи витрати на дзвінки та підвищуючи конверсію.

**Основна метрика:** ROC-AUC (для оцінки здатності моделі ранжувати клієнтів).
**Бізнес-метрика:** Баланс між кількістю залучених клієнтів та кількістю "зайвих" дзвінків.

## Дані
Використано датасет [Bank Marketing Dataset](https://archive.ics.uci.edu/ml/datasets/bank+marketing) (версія `bank-additional-full.csv`).
Він містить інформацію про демографію клієнта, історію попередніх контактів та макроекономічні показники (Euribor, індекс цін тощо).

## Хід виконання роботи

### 1. Exploratory Data Analysis (EDA)
Проведено аналіз розподілу ознак. Ключові інсайти:
- **Сезонність:** Виявлено залежність успіху від місяця (травень — найнижча конверсія).
- **Економіка:** Ставка Euribor має сильну зворотну кореляцію з успіхом кампанії.
- **Попередній досвід:** Успіх у попередній кампанії (`poutcome=success`) значно підвищує шанси на успіх у поточній.

### 2. Препроцесинг
- Обробка пропущених значень (`unknown`).
- Створення нових ознак: `age_group` (вікові групи), `is_prev_contacted`.
- Логарифмування розподілів зі скошеністю (`campaign`, `previous`).
- Кодування категоріальних змінних (OneHotEncoding) та масштабування числових.

### 3. Моделювання
Було протестовано 4 алгоритми. Результати на тестовій вибірці:

| Модель | Train AUC | Test AUC | Коментар |
| :--- | :---: | :---: | :--- |
| **Logistic Regression** | 0.7932 | 0.7910 | Стабільна базова модель |
| **kNN** | 0.8510 | 0.7450 | Значне перенавчання, повільна |
| **Decision Tree** | 0.8120 | 0.7240 | Схильна до перенавчання |
| **XGBoost (Tuned)** | **0.8250** | **0.8015** | **Найкращий результат після Hyperopt** |

*Примітка: Значення метрик є орієнтовними (вставте ваші точні цифри).*

### 4. Оптимізація та аналіз помилок
Для найкращої моделі (XGBoost) було проведено:
- **SHAP Analysis:** Визначено, що найбільш впливовими факторами є `nr.employed`, `euribor3m` та `poutcome`.
- **Error Analysis:** Виявлено, що модель надто консервативна і "боїться" дзвонити клієнтам у періоди високих ставок Euribor, пропускаючи платоспроможних клієнтів.

## Висновки та Бізнес-рекомендації

### Ключові досягнення
В ході аналізу помилок було встановлено, що стандартний поріг прийняття рішення (0.5) є неефективним для бізнес-цілей банку, оскільки призводить до втрати великої кількості потенційних клієнтів (False Negatives).

Ми провели експеримент зі зниженням порогу до **0.3**. Результати:
- **+83%** знайдених реальних клієнтів (ріст True Positives з ~250 до ~460).
- Це коштувало банку лише **+338** додаткових дзвінків, що є економічно виправданим обміном.

### Рекомендації
1. **Впровадження:** Використовувати модель XGBoost з порогом класифікації **0.3**.
2. **Стратегія:** Розробити окремі скрипти розмов для клієнтів з "холодного" сегмента (без попередньої історії), оскільки модель показує на них нижчу впевненість.
3. **Покращення:** В майбутньому варто розділити модель на дві: одну для нових клієнтів, іншу — для тих, з ким вже контактували.

---
*Автор: [Ваше Ім'я]*
